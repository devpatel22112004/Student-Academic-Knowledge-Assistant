# Virtual environment is machine-local and reproducible from requirements.
.venv/

# Python bytecode/cache files are build artifacts (never source of truth).
__pycache__/
*.pyc

# Generated outputs are reproducible and can be recreated by runner scripts.
outputs/

# Large/raw academic PDFs may be private or too heavy for repository history.
data/pdfs/*.pdf
