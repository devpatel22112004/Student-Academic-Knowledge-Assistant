# ===== REQUIRED (Original Project) =====
# Required for core extraction + indexing pipeline.
# langchain-text-splitters: text ko chunk size/overlap ke saath split karne ke liye.
langchain-text-splitters
# faiss-cpu: embeddings ko vector index me store/search karne ke liye.
faiss-cpu
# sentence-transformers: chunk/query text ko embedding vectors me convert karne ke liye.
sentence-transformers
# pypdf: PDF files se text extract karne ke liye.
pypdf
# numpy: embedding arrays ko numeric matrix format me handle karne ke liye.
numpy
# python-docx: .docx Word files ka text read karne ke liye.
python-docx
# striprtf: .rtf files ko plain text me convert karne ke liye.
striprtf

# ===== ADDITIONAL (OCR Fallback - Our Enhancement) =====
# Optional OCR support for scanned PDFs.
# Also install system tools: `tesseract-ocr` and `poppler-utils`.
# pytesseract: image-based text (OCR) read karne ke liye.
pytesseract
# pdf2image: PDF pages ko image me convert karne ke liye (OCR step se pehle).
pdf2image
# Pillow: image processing dependency (OCR pipeline support).
Pillow
